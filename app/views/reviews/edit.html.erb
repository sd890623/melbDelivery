<h1>Editing Review</h1>

<%= render partial: 'form', locals: { shop: @review.shop, review: @review } %>

<%= link_to 'Show', [@review.shop, @review] %> |
<%= link_to 'Back', [@review.shop, Review] %>
